; ===========================================================
; OpenPBX - PJSIP メイン
; transport / template / endpoint は すべて pjsip.d/*.conf として
; Web (/extensions, /network) から動的生成・reload される。
; このファイルは include のみ。手で編集する必要は基本ない。
; ===========================================================

#include "pjsip.d/transports.conf"
#include "pjsip.d/extensions.conf"
#include "pjsip.d/trunks.conf"
